# LiteLLM Proxy server URL (running locally on port 4000)
LITELLM_PROXY_BASE_URL=http://localhost:4000/v1

# Optional key (ignored by LiteLLM proxy, but required by LangChain’s OpenAI-compatible wrapper)
LITELLM_API_KEY=ignore

# Claude model version
MODEL_NAME=claude-3-5-sonnet-20240620

# Other optional config
TEMPERATURE=0.2
MAX_TOKENS=4096
